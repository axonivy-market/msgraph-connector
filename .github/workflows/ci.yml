name: CI-Build

on:
  push:
    branches:
      - test-ci
  schedule:
    - cron: '*/5 * * * *'  # every 5 mins
  workflow_dispatch:

jobs:
  build:
    if: github.ref == 'refs/heads/test-ci'
    runs-on: ubuntu-latest
    steps:
      - run: echo "⏰ CI on test-ci only"
